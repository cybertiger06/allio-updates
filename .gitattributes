# Git LFS para archivos pesados (JARs grandes, ZIPs)
*.jar filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Binarios - no hacer merge
*.jar binary
*.zip binary
*.png binary
*.ogg binary
*.nbt binary
